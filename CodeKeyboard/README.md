# CodeKeyboard (React Native app)

See [root README](../README.md) for the full project overview.

## Running the RN app

```bash
npm start
npm run android
```

The IME lives in `android/app/src/main/java/com/codekeyboard/` as a native Kotlin service. The RN side is the in-app keyboard and settings UI.
